<!--
SPDX-FileCopyrightText: 2026 HaoHao IME contributors

SPDX-License-Identifier: GPL-3.0-or-later
-->

# 好好输入法路线图

这份路线图描述项目方向，不代表固定发布日期。每个阶段都以可构建、可回归和不破坏原版中文输入为前提。

## 阶段 0：原版基线

- [x] 拉取 Trime `develop` 及全部递归子模块
- [x] 使用隔离的 JDK、Android SDK、NDK、CMake 和 Gradle 缓存完成 Debug 构建
- [x] 修复 Windows 符号链接检出问题，确认 Rime 配置和词典被正确打包
- [x] 在 Android API 36、x86_64 模拟器部署“朙月拼音·简化字”
- [x] 验证“你好”“中国”、中英文切换和 Logcat 稳定性

## 阶段 1：开源项目基础

- [x] 建立公开 GitHub Fork 和独立 `main` 分支
- [x] 建立中文优先的项目说明与上游归属说明
- [x] 建立 ARM64、x86_64 轻量 Debug CI
- [x] 统一“好好输入法 / HaoHao IME”名称、标语与应用图标
- [ ] 整理贡献规范、Issue 模板和开发文档

## 阶段 2：中文用户体验

- [x] 全新安装默认启用“朙月拼音·简化字”，无需首次选择方案
- [x] 新增简洁的“好好 26 键”主键盘、数字页和常用符号页
- [x] 保留原版主题与其他方案文件，且不覆盖已有用户选择
- [ ] 评估更现代的简体中文词库与候选排序方案
- [ ] 优化中文界面文案和常用设置入口
- [ ] 建立中文输入回归用例集

## 阶段 3：双语候选

- [x] 调研 Trime 候选数据流和最小接入点
- [x] 定义中文候选与英文翻译的展示模型
- [x] 使用内置离线演示词表实现可关闭的双语候选原型
- [x] 接入固定版本 CC-CEDICT、确定性短释义生成器和只读内存映射查询
- [x] 处理多义词覆盖、结构说明过滤、无翻译结果和损坏文件降级
- [x] 展开候选采用三列双语网格，并过滤常用 Android 字体无法显示的扩展汉字
- [x] 候选停止变化后按用户设置防抖显示英文，等待和未命中状态保持统一行高
- [x] 支持竖屏、横屏紧凑栏候选数量上限，展开页继续保留完整 3×3 候选
- [x] 接入固定版本美式 IPA 数据，并提供默认关闭的第三行音标开关
- [ ] 继续优化固定短语覆盖与多义词语境选择
- [x] 保证关闭功能时与原版 Trime 行为一致

已验证的架构约束：翻译数据只进入候选展示模型，不写入 `CandidateProto`、Rime API 或 JNI；候选数量、排序、索引与提交内容必须保持原样。展开候选若为可读性过滤生僻扩展字符，展示项必须携带原始 Rime 全局索引，禁止使用过滤后的列表位置执行选择或长按操作。翻译词表缺失或解析失败时，展示层必须降级为无翻译，不能阻断中文输入。

已验证的默认配置约束：默认方案和主题只在用户尚未保存选择时生效；Rime 方案仍由数据目录中的 `default.custom.yaml` 管理。字母方案会由 Trime 自动匹配 `qwerty` 预设，因此自定义主键盘必须同时为 `qwerty` 提供到 `default` 的主题内映射。

## 阶段 4：翻译、隐私与性能

- [x] 优先使用随 APK 内置的离线词典，不增加网络权限和首次下载
- [x] 采用内存映射索引控制候选查询延迟与 Java 堆占用
- [x] 建立词典缺失、版本错误和数据损坏时的无翻译降级
- [ ] 评估由用户主动开启的 Android 系统翻译能力
- [ ] 评估由用户主动配置 Key 的可选云 API，并明确数据上传和隐私提示

## 阶段 5：真机与发布

- [ ] 在主流 ARM64 Android 真机验证安装、权限和输入法切换
- [ ] 验证触屏延迟、横竖屏、全面屏和不同厂商系统兼容性
- [ ] 完成签名、版本号、发布说明和可重复 Release 构建
- [ ] 真机验收通过后再发布首个稳定测试版本

## 当前测试边界

项目目前以 `Trime_API_36` 模拟器作为主要交互验收环境。模拟器适合验证功能、界面和崩溃问题，但不能完全替代真机上的触控体验、厂商权限管理、耗电与 ABI 兼容性测试。
