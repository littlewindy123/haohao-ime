# 学习页与句子本 v4 验收

## 本轮范围

- 官网已上线改动先单独提交并推送：`4bc3b128`。
- App 3.3.13 / 20260925：学习卡答案区域局部更新、固定反馈区域、慢速按钮短标签；详情补充独立离线资料。
- Room 3→4 增量新增句子表与开关表，旧单词、用户确认的词义、评分、计划、撤销和会话不改写。
- 句子本默认手动收藏；主动同意后自动保留最近 100 条未收藏对照，收藏不淘汰。密码/禁止个性化学习编辑框不记录。模式变更、清空、会话变更及隐藏键盘会使旧写入失效。
- Rime 提交事件携带当时的不可变译文快照，仅匹配实际成功上屏的中文；独立译成英文模式只在提交成功时记录原对照。无新翻译请求，无编辑框历史读取。
- 数据放现有 `noBackupFilesDir` 数据库，不进入系统备份、日志或网站。句子不会加入单词复习。
- 本轮没有修改云服务、官网 APK、下载元数据或键盘候选布局。

## 离线资料

独立 SQLite 只读库 30,000 词头，显式词形索引；9,674 个词含例句，共 21,163 条按词索引的例句，每词最多 3 条。包含义项、音标、英文定义、词形及来源。不存在义项级双语配对时，英文定义单独展示；用法例句也不冒充已保存词义的精确示例。

源码快照 4,129,771 字节，数据库 16,805,888 字节。固定来源、校验和、授权、重建规则见 [离线资料说明](../../app/dictionary/learning/README.md)。抽检 learn、bank、run、book、set、hello、apple、study；保留来源的简/繁中文，不伪造或重写缺失例句。

## 测试结果

- `:app:testRegressionUnitTest`：265 项通过，0 失败、0 跳过。包括词条/句子长度边界、输入隐私策略、不可变提交快照及错配隔离、原有翻译和输入流水线回归。
- `SentenceStoreTest` + `InputFootprintStoreTest`：真机 22 项通过。覆盖 Room 1/2/3→4、旧答案和撤销保留、默认手动、101 条淘汰、收藏保护、相同对照去重、不同译文并存、关闭/清空过期任务、事务中失效回滚、离线资料不改旧答案。
- `WordLearningUiTest#wordsAndReviewRemainUsableAndPersistAcrossRecreation`：真机通过。翻面、反馈、撤销、页面重建、每日计划与反向复习揭晓前不显示发音。
- `LearningV4UiTest#reviewGeometryAndSentenceSearch`：浅色普通字号、360dp/1.3 倍字号深色、横屏/1.3 倍字号深色分别通过。确认翻面不重建 ScrollView、常规竖屏反馈按钮位置不变、按钮至少 56dp；横屏滚动到底后反馈按钮完整可见。句子搜索和收藏分类在重建后保留。
- `python app/dictionary/learning/test_lexicon.py`：3 项通过，验证所有 30,000 条快照与 SQLite 一致、文件校验和、去重、直接例句署名字段、整词/词形匹配和常用词。
- OEM 会拦截测试进程从后台启动新页面；测试中先通过现有主页面进入前台，再正常打开非导出的学习页面。未放开正式 App 的组件权限。
- 全部手机测试使用独立 `com.osfans.trime.local.regression` 的固定数据，不发送聊天消息。已恢复 1200×2670、字号 1.0、竖屏及用户原输入法。

## 产物与边界

本地 `haohao-ime-3.3.13-20260925-arm64.apk`，59,003,208 字节；SHA-256：`de5a469b421f07050ea363d9461a46cccc921e184789f02fc88c3111d4d9ff68`。固定签名为 `public-signing.properties` 的现行证书；APK 全项扫描未发现本机已知云凭据、签名口令或迁移口令。该包不内置共享云密钥，未宣称新云能力上线，未覆盖安装到日常 App，也未上传官网。

真机测试不包含真实云服务调用或私人聊天中的长时间自动记录；快续输、迟到译文和会话隔离主要由现有翻译回归及新增快照/存储测试覆盖。例句没有逐条人工语义标注或义项配对，后续仍可继续做内容质检。

最终迁移包为 `haohao-migration-final.7z`，4 个文件，包含现行固定签名及其容器口令、本地两项内测配置；随机解压口令单独存本机文本。已验证 AES、文件名加密和逐文件解密一致；不包含 SSH/云主账号/学习数据。初次 `ready` 包误取早期签名，已明确作废，请只使用 `final` 包。[另一台电脑搭建](second-computer.md)。
