# 内测发音、学习和大字候选（当前交付 20260921）

## 2026-09-06 22:35 / 20260921 紧凑排版与三套配色

- 按用户红框缩减候选区留白：中文行48→40dp，中文与字母22→20sp，英文14→13sp；固定两行英文占32dp，整块82→72dp。长句仍按自然字宽挤开后续候选，不随内容缩字、不随译文到达改高度。紧凑键帽尺寸、高度/单手偏好未改。
- 使用显式 `system:sans-serif` 常规字重，覆盖候选、按键、英文、辅助符号和拼音。手机原先未安装主题字体，本来就回退系统字体；本次主要可见差异是字号和比例调整，不能声称换成了搜狗专有字体。手机系统配置显示MiSans映射；搜狗官方FAQ没有确认截图中的精确字体名称。未提取/分发搜狗字体资产。
- 右侧展开图标18→32dp，修正CENTER_INSIDE不放大矢量图的问题；点击区域固定48×48dp。26键及数字键盘的“符”独立设为16sp。
- 新增雾蓝、暖杏、石墨及各自夜间配色；延续原布局，所有主文字/译文/回车文字对比度测试至少4.5:1。主题版本3.1。好好主题配色在选择器置前，保留继承的旧配色；选择颜色后立即按当前日夜模式解析配对颜色。
- 路径：主应用向下滚动 → 键盘外观与主题 → 配色。

### 本次验证

- Debug、Regression、AndroidTest构建成功；257项JVM测试通过，含新增字体/符键参数、三套配色完整性/对比度/日夜配对、列表排序回归。
- 真机隔离视图测试3项通过：20sp不缩字（1/1.3/2倍字号）、13sp两行英文长句/省略与到达时布局稳定、短窗口测试输入控件。未把隔离视图测试表述为所有真实主题/横屏场景实测。
- 主应用实测短词“输入法 → Input method”、新箭头、缩小的“符”；从设置切换暖杏后键盘配色正确。当前暂留暖杏供用户查看。
- 手机通话Activity多次取得前台，焦点保护中止后续操作；雾蓝/石墨的真实键盘截图、夜间切换和完整长句输入链尚未完成真机验收。未操作聊天、未点播声音。系统字号/旋转设置未改动。
- 最终内测APK已覆盖安装，包名及证书不变，首装时间仍2026-09-05 23:50:49；无卸载/清空主应用。清理了本次安装的两个隔离regression测试包（可从本机构建产物重装），主应用词本/设置保留。
- 未提交/推送Git，未上传官网/COS；腾讯云TTS接入状态不变。

### 交付文件

- `C:\Users\ADMIN\.haohao-ime\internal-test\haohao-ime-20260921-internal-arm64.apk`
- SHA-256 `2280fc838bb8a573293f72e94f6d485d9fda38033b2bf3bf2c4b4f0cd9ed304c`
- 固定证书 SHA-256 `6278edd3637cf54377d63f78f7134ac1ab6e4b5b3721229719201e8262ab3215`。
- 包内语音配置隔离和主题3.1核验通过。以下为历史记录，不代表本轮重复验收。

## 2026-09-06 晚间真机调试 / 20260920

用户恢复真机调试，随后明确反馈：单字到句子需要统一，候选右侧不应大片留空。

- 句子优先模式不再受旧的竖屏4条/横屏6条目标数量限制，按视口和最小单元宽度取有界候选池。首句占自然宽度，继续展示后续候选至边缘；剩余不足48dp也能露出下一条的一部分。其他翻译模式及用户保存的数量设置不改写。
- 英文短词和整句均从相同的左侧、顶部基线开始，中文保持22sp；翻译到达不重新分配中文宽度。中文和按键位置在单字、短词、长句截图中保持一致。
- 横屏尺寸适配与“是否启用横屏键位布局”解耦；HaoHao物理横屏使用横屏边距与高度，不修改其他主题行为，也不改写用户高度/单手选择。主题版本3.0，横屏基础键盘180dp，底部留白6dp；竖屏参数不变。短窗口测试输入页将类型选择、输入框与关闭操作放在一行。
- 横屏辅助数字/符号移至键帽右上角，避免与主字母叠放。此最后一项仅编译验证；后续旋转请求未取得实际横屏画面，不标记视觉验收通过。

### 已通过

- 20260918 → 20260919 → **20260920** 均 `adb install -r` 成功。包名与固定证书不变，实际包首装时间仍为2026-09-05 23:50:49，未卸载/清空用户主应用。
- 除本机254项JVM回归之外，Android真机隔离视图测试3项通过：候选不缩字（1/1.3/2倍字号）、短词/长句译文到达布局稳定、短窗口输入控件可见且可触达（1/1.3倍字号）。JVM最终254项、0失败/0错误；服务器测试仍为上一轮20项模拟测试，本轮未重新调用语音云服务。
- 主应用自带测试页：`你`约7项、`你好`约5项自然铺满候选行；`你好我今天想去公园散步`首选自然挤开后续候选，英文整句正常显示。展开中文候选的按钮可点击，并在键盘内部显示完整首句。
- 1.3倍系统字号下 `我爱你 → I love you` 未隐藏、中文未缩小。字体测试结束恢复系统1.0倍。
- 20260919真机横屏：测试栏完整显示；数字框自动切换数字布局，点击得到精确的`12.3`，长按退格清空固定测试文字。
- 自动旋转及user_rotation已恢复调试前值（1和0）；没有触碰聊天输入或发送消息。

### 尚未通过 / 保持隔离

- 两个单词卡ActivityScenario测试未完成：运行停在界面启动阶段，未出现其目标活动。停止的是隔离regression进程；日志中的`Process crashed`对应主动停止，不是主应用崩溃证据。未把翻面/评分/撤销流程记为本轮通过，原因还需定位。
- 手机有通话悬浮窗，为避免抢占音频焦点，没有点播声音，也未进行耳机/焦点实测。腾讯云发音门控未打开，仍需下述云接入条件。
- 没有新的每帧/延迟测量，不能据截图宣称已达到搜狗的全部流畅度。1.3倍字号下键帽辅助符号间距仍值得后续精调。
- 不提交/推送Git，不上传官网/COS；只覆盖安装内测包。
- 测试结束卸载本轮创建的`com.osfans.trime.regression`及其测试运行器，只清理了隔离测试词本；主应用`com.osfans.trime.debug`保留。测试APK仍在本机，可重新安装复测。

### 当前交付

- `C:\Users\ADMIN\.haohao-ime\internal-test\haohao-ime-20260920-internal-arm64.apk`
- SHA-256 `1b095919d51f99f27bcb95802379ac3b3b6f8cb3899beb78c7c11f21f42a2e80`
- 固定证书 SHA-256 `6278edd3637cf54377d63f78f7134ac1ab6e4b5b3721229719201e8262ab3215`。
- 内测端点与令牌仅出现在内部APK；回归包不包含它们。应用包无测试音频、无腾讯云密钥特征。下面20260918段落保留为历史实施记录。

## 2026-09-06 实施范围

- 中文候选 16 → 22sp、候选行 30 → 48dp；英文 12 → 14sp，独立两行。字母键 20 → 22sp，紧凑键帽高度及用户高度/单手选择保持不变。
- 首句先占自然宽度；继续输入时挤走后续候选。取消原来的 0.85 比例缩字。最后一条可局部露出，显示其开头；首句超过整行才在自己的视口查看尾部。更多候选入口保留。
- 展开控件 48dp，对齐中文行，不再垂直居中于中英两层；英文整句使用包含控件下方的完整行宽。译文到达不调整中文字宽、候选行高度或键帽高度。
- 主题资源版本 2.9，沿用奶油白、薄荷绿、蜂蜜金、可可棕；首选用绿色文字强调，取消句子模式的整块高亮底色。
- 手动英文发音入口：单词详情、复习、候选长按、句子全文及“译成英文”结果。独立授权；正常/慢速；全局播放互斥、取消、焦点丢失/拔耳机停止。
- Android 15+ 临时 mediaPlayback 前台服务，通知无句子正文，非开机启动、非自动朗读。
- 音频缓存 noBackupFilesDir，50MiB/7天/LRU、内容散列命名，撤销授权仍可播已有缓存；缓存清空隔离迟到请求。单段450字符，总长2000，按标点/词界分段。
- 主应用复习为主卡片与底部动作；翻面前英文侧可点播，反向复习隐藏英文发音。短窗口将底部操作纳入滚动，评分和复习数据模型不变。

## 服务器已部署（不代表腾讯云发音已启用）

- `POST https://124.221.187.214/api/v1/speech`，本地 Uvicorn `127.0.0.1:18761`，systemd `haohao-speech`。
- 官网 HTTP 保留；HTTPS 引用同一个静态发布目录，未改官网文件或下载包。
- Let's Encrypt 受信任 IP 短期证书；本次证书有效至 2026-09-13 02:13:33 UTC；`haohao-certbot-renew.timer` 每日两次检查续期，已完成 staging 和 dry-run 续期/重载。
- 服务程序 `/opt/haohao-speech/current`，环境 `/etc/haohao-speech/service.env`（0600），字符额度数据库 `/var/lib/haohao-speech/quota.sqlite3`。
- 配置默认关闭实际云调用；不正确令牌返回401 UNAUTHORIZED，正确内测令牌返回503 NOT_CONFIGURED，外网 HTTPS 检查已通过。未调用腾讯云合成、未购买或开启后付费。
- 限定 WeJack 101050，MP3/16kHz；2并发、10次/来源/分钟、50000字符/日，调用前SQLite原子扣减，重启不重置。日志不输出正文和凭据。
- 临时客户端令牌可通过更换服务器 token 撤销；腾讯云凭据绝不进入 APK。公开/回归构建的端点与内测令牌强制清空。

## 云端待办

用户截图已证实通用语音合成免费包800万字符、已用0；右侧有效期和后付费状态尚未核实。还需要受限、可撤销、仅允许 TTS 的腾讯云凭据，在服务器0600配置中提供，不要发进聊天或源码。确认免费边界后才能设置 `SPEECH_FREE_ONLY_VERIFIED=true`、实际免费截止时间、专门分配的字符总量。截止时间不得超过2026-09-30（与既有内测日期语义一致，2026-10-01 00:00 UTC拒绝）。不得用COS预算购买TTS。

## 验证状态

- 最终 JVM 回归 251 项通过；服务端模拟测试 20 项通过；Debug、Regression 和 AndroidTest APK 均构建成功。修改范围的 Kotlin 格式整理及 `git diff --check` 已通过。
- 新增请求取消/晚到结果丢弃、TLS配置拒绝、超时、不重试、错误码、缓存TTL/LRU、限流并发与重启保留等测试。
- Android隔离测试包含单词卡持久化/撤销/反向无答案泄露，以及私有缓存播放/停止。`speech-tone.mp3` 是离线生成的低音量测试音，不是腾讯云英文语音，不可据此宣称音色已实测。
- 安装隔离测试包被手机返回 INSTALL_FAILED_USER_RESTRICTED。随后用户明确要求外出期间不测试、不请求安装；故新界面、真实音频焦点和耳机/横屏真机验收留待用户回来。不得标记已通过。
- 不清除现有 App 数据，不提交/推送 Git，不上传官网或COS。当前包固定签名，升级只用 `adb install -r`，不卸载正式内测包。

## 已生成的交付文件（未安装）

- `C:\Users\ADMIN\.haohao-ime\internal-test\haohao-ime-20260918-internal-arm64.apk`
- 包名 `com.osfans.trime.debug`，版本号20260918，版本名3.3.12。
- SHA-256 `aceeb878b7e876eb8fc5a3f3f96b02c0a044e219860e45aab6111b4b9bc77041`。
- 固定签名证书 SHA-256 `6278edd3637cf54377d63f78f7134ac1ab6e4b5b3721229719201e8262ab3215`；v1/v2 签名验证通过。
- APK 核验：主题2.9、内部端点与访问令牌仅出现在内测包；Regression 包不含该端点或令牌；两种应用包均不含测试音频或腾讯云密钥特征。

## 官方依据

- [Tencent TextToVoice](https://cloud.tencent.com/document/product/1073/37995)
- [Certbot IP short-lived certificates](https://letsencrypt.org/2026/03/11/shorter-certs-certbot)
- [Android audio focus](https://developer.android.com/media/optimize/audio-focus)
- [Foreground-service start exemptions for the current IME](https://developer.android.com/develop/background-work/services/fgs/restrictions-bg-start)
