# 词书到今日学习 — 3.3.26 / 20261011

本轮后续改动仅涉及学习入口与页面状态；官网已发布的是独立冻结、经过真机验收的 3.3.25 / 20261010。没有修改复习算法、数据库结构、候选交互或云服务，也没有执行 Git 提交/推送。

## 交互

- 词书默认浏览，不显示每行复选框与批量管理操作。学习与管理分开，已存在学习会话时显示“继续学习”。
- 没有学习词时主按钮进入选词；只确认所选 N 词快照，不默认将整本词书加入学习。原整本加入仍是管理模式下的显式操作。
- “今日学习”使用全局现有任务，不是按词书过滤的新队列。已固定的今日目标不被新增词扩大，未完成会话沿用原卡片与进度。
- 未开启计划时必须主动开启并保存；取消或保留关闭不创建学习会话。原计划数量与模式不被重置。
- 旋转保留选择与未保存的计划草稿；取消不写入草稿。后台完成选词写入后，在页面恢复时仅跳转一次。
- 请求保持至数据库创建/恢复会话和页面显示完成；挂起期间重建不会丢失开始请求。

## 验收口径

使用临时隔离包 `com.osfans.trime.regression` 与其测试 runner，测试入口强制检查目标包，不操作日常版学习数据。测试数据只含自建词本、固定词与内置词书。

- 333 项 JVM 测试通过（包含 5 项新增入口策略测试）。
- 浅色完整 `WordbookUiTest` 10 项通过：原导入/建词本/管理流程、新选词确认与取消、首次开启计划、旋转/后台恢复、原设置/会话/今日目标保留。
- 最终浅色完整 10 项、深色横屏/1.3 倍字体 3 项、约 360dp 竖屏/1.3 倍字体 3 项均通过，总计 16 次真机 UI 检查。
- 中途测试暴露了截图遍历/重新布局后的滚动定位，以及未等待 Room 异步加载就断言列表的问题。测试现在等待完整可见、等待准确的 fixture 列表数量，仍发送真实 pointer 事件并保留完整宽高与文本裁切断言；未以放宽断言或替换直接 performClick 的方式跳过验证。后台 fixture 为 2 词，显式选择 1 词。

已查看最终浏览、选词、首次计划截图：无学习词时不再出现重复“选词学习”；浅色窄屏大字体无文字裁切，深色横屏可以滚动访问完整主按钮。没有把统计页或整个 App 视觉重做计入本轮成果。

原始测试报告与截图位于仓库外 `../work/input-feel-20260914/`，不包含日常词条或聊天。最终报告为 `learning-final-light-20260915.log`、`learning-final-dark-landscape-20260915.log`、`learning-final-light-large-20260915.log`；最终截图在 `learning-ui-final/`。

临时隔离包及 runner 已卸载，测试数据可从固定 fixture 重建；截图与测试报告保留在本机工作目录。手机仅保留 `com.osfans.trime.debug`，已通过 `adb install -r` 覆盖至 3.3.26 / 20261011。没有卸载日常包或清空日常数据，恢复原物理密度 520 和字体倍率 1.0。

本机产物 `../work/haohao-ime-3.3.26-20261011-private-arm64.apk`，57,739,354 bytes；SHA-256 `7dcf2875107149afb08f4ca0afab57b1c96e3d3586bfad4dceb9d6b5e52e3e45`。固定签名 `62b4a4c620df03d8bd6c65e21a8cfa5cd11265465e25070dc53006b31de5a101` 核验通过，原内部配置与 2026-09-30 截止日期保留。该产物不上传网站或 Git；官网仍是已验收的 3.3.25 公开包。

## 日常包最终输入复测

覆盖安装后在自带测试页，以已测量的 26 键坐标再次发送真实触摸：`nihao` 5 字母与长句拼音 132 字母，均为 12 Hz。两组处理增量分别为 5、132，预编辑拼写完整一致；停止后首次采样队列均为 0。报告为 `daily-final-qwerty-3-3-26-short-12-hz.json` 与 `daily-final-qwerty-3-3-26-extended-12-hz.json`。

此前两组收尾尝试分别遇到键盘尚未挂载、实际处于九键但使用 26 键坐标的问题，属于测试前置条件不满足，不纳入性能通过/失败结论。已通过键盘方案菜单恢复用户原先的“拼音全键”，并为触摸脚本补充空文本、焦点、已测量输入框位置与 `luna_pinyin_simp` 方案检查。进程累计最大值包含这些无效前置尝试，本次复测只使用计数增量、准确拼写及结束队列状态，不将累计最大等待值当作本轮性能数据。

复测后已清空合成测试输入并移除 JDWP 转发；未发送聊天消息，暖杏主题保留。再次核验手机仅有日常包，版本 3.3.26 / 20261011、密度 520、字体倍率 1.0、默认输入法不变。

未覆盖：强杀进程后的完整恢复、存储耗尽/持久化错误的真机重试、多机型、端到端触摸显示时延及独立冷启动基准。配置重建与命令队列测试不能代替这些场景。
