---
name: 好好输入法
description: 温暖但精致的 Android 输入与个人词本
colors:
  page-light: "#F7F4EC"
  page-dark: "#1D1C19"
  surface-light: "#FFFDF8"
  surface-dark: "#292723"
  honey-light: "#F4BF61"
  honey-dark: "#E0A947"
  on-honey-light: "#513A32"
  on-honey-dark: "#3D2C26"
  selection-light: "#DCECE2"
  selection-dark: "#354D41"
  text-light: "#513A32"
  text-dark: "#F4ECE2"
  secondary-text-light: "#756359"
  secondary-text-dark: "#C6B8AA"
  divider-light: "#E3DDD2"
  divider-dark: "#45413A"
spacing:
  control-gap: "8dp"
  content: "16dp"
  reading: "24dp"
---

# Design System: 好好输入法

## Overview

**Creative North Star: "温暖但精致"**

本规范记录 Android 原生好好界面及个人词本已经落地的视觉语言。奶油色底、薄荷色选中态、蜂蜜色行动与可可色文字延续用户确认的金毛品牌；输入和学习内容保持安静、清楚。上游高级设置和用户自定义 Rime 主题不因此被强制替换。

**Key Characteristics:**

- 温暖的低饱和底色，浅深主题分别定义。
- 英文义项、中文释义、来源和状态形成清晰层级。
- 列表以留白和细分隔线组织，金毛只用于欢迎、空态和复习完成。

依据：[产品约束](PRODUCT.md)、[原生设计示意](design-demos/haohao-learning/native-words.html)、[浅色资源](app/src/main/res/values/colors.xml)、[深色资源](app/src/main/res/values-night/colors.xml)。示意中的示例数据不是使用记录；原生代码和设备截图优先于 HTML 示意。

## Colors

蜂蜜色承载主要行动；薄荷色表示选中或品牌区域；页面、内容表面、主次文字与分隔线使用成对的浅深主题资源。顶部品牌薄荷色与列表选中薄荷色使用不同资源，不能互相替代。

**The 主题配对 Rule.** 原生控件引用 `haohao_*` 颜色资源；改动浅色角色时同时检查 `values-night` 对应角色。蜂蜜色按钮使用 `haohao_on_honey`，不能直接复用普通主文字色。

**The 次级可读 Rule.** 次级文字通过字号和颜色表达层级，不能以低对比度代替层级。`secondary-text-light` 在薄荷选中面、奶油页面与暖白内容面上的普通文字对比度均达到 4.5:1；摘要说明和输入提示复用这一角色。

学习阅读使用现有 `learning_*` 成对资源：英文为绿，中文释义为棕红，阅读操作为蓝；模式选中保留蜂蜜色。复习反馈以红、琥珀、绿区分「忘记了」「有印象」「记住了」，揭晓和核对使用绿色，均搭配对应前景色；状态同时用文字表达。资源来源为浅深主题的 `learning_reading_colors.xml`。

## Typography

使用 Android 系统文字与 `sp` 字号，遵循系统字体缩放。列表中英文义项为粗体（23sp），中文释义为正文（15sp），音标与次数为次级信息（13sp、12sp）。详情主词为 34sp；复习英文题面为 46sp，中文题面为 34sp，答案和拼写输入为 24sp。这些是各页面的当前实现尺寸，不是可任意套用的全局展示字号。

普通操作按钮为 15sp，复习反馈按钮为 14sp。长义项可在列表中省略，完整阅读交给详情和可滚动复习页；不能通过缩小正文解决空间问题。候选英文、音标行高随系统字号缩放，候选栏同步预留空间；英文淡入不改变中文位置、候选顺序或点击区域。

词本管理沿用系统 `sp`：目录与预览标题为 24sp、词本名为 26sp、词条为 17sp、按钮和输入为 16sp、计数和辅助信息为 13–15sp。词条中的英文与中文释义直接换行，复选框和文本区域随内容增高。学习首页保留 28sp 标题、36sp 粗体数量和 14sp 数量标签；这些页面角色不替代既有复习题面的字号。

**The 键面容纳 Rule.** 内置好好主题在 fontScale > 1 时，经典与立体键帽均先按平台 SP 转换取得目标字号，再在实际键面内分别安排主文字、副符号、提示与图标，必要时缩放到各自区域，避免字形相碰或越界。默认字号和自定义主题保持原排版；此适配仅用于固定键面，不能据此缩小学习页正文。

## Layout

「最近遇见」「我的收藏」「正在学习」列表采用原生工具栏、可折叠介绍区、三项筛选、搜索框与单层列表。列表左右留白为 16dp；详情和复习的内容左右留白为 24dp。按钮组使用 8dp 间距。搜索框高 56dp，图标触控区为 48dp，复习按钮最小高度为 48dp。

词本横屏默认收起介绍区，保留筛选和搜索；详情、计划和复习使用 `ScrollView`，并处理系统栏及 IME insets。增大字号和旋转屏幕时，优先保留可滚动内容及触控面积。

复习入口用简短动作文字，数量只放在摘要；并排入口关闭基线对齐并保持等高。发音操作和状态提示分成两行，控件及父容器均按内容增高。四种模式采用两行两列按钮，附当前模式说明；评分、查看答案和拼写输入使用 56dp 最小高度，按文字内容增高。

**The 输入可见 Rule.** 短窗口（高度小于 480dp）、大字号（fontScale ≥ 1.8）或拼写模式下，揭晓与评分区域随内容滚动，不重复叠加页面左右留白。短窗口展开 IME 时暂时隐藏工具栏，拼写输入上下内边距从 12dp 减至 4dp；收起 IME 后恢复。系统栏与键盘 insets 共同参与布局，并滚动至输入框全文可见；重建保留草稿，不自动聚焦或弹出键盘。

备份与恢复采用工具栏下的原生单列滚动页，说明、状态、预览和操作按阅读顺序排列；长说明分段，按钮全宽、最小高度 56dp，并随文字增高。薄弱词的两个分组在同一滚动页依次展开，各自保留真实总数、当前范围和翻页操作；窄屏、大字号与横屏均通过内容增高和滚动保持操作可达。

词本管理、内置词书目录和导入流程采用 56dp 原生工具栏加一个 `ScrollView`，内容左右留白为 24dp，按钮之间留白为 12dp。操作按钮全宽、输入与按钮最小高度为 56dp；复选框宽度和词条触控最小高度为 48dp，行高随文字增加。词本详情先显示名称、真实计数、「整本加入学习」和每日数量入口，再显示搜索与平面词条列表；页码和批量操作位于列表之后，全部在同一滚动内容中。

学习首页以薄荷摘要容纳数量、进度和当前主操作，下方显示学习模式及管理入口。默认手机字号优先呈现选书或继续学习；短窗口与大字号允许首屏只容纳部分摘要和操作，以滚动可达为准，不压缩正文或固定按钮高度。词本管理和导入页共同处理系统栏与 IME insets，重绘不主动弹出键盘。

## Elevation & Depth

好好键盘提供“经典 / 立体”两种按键样式，独立于四套配色，默认经典并保留旧版外观。立体键帽在原按键内部绘制最多 3dp 厚度，按下最多移动 2dp；厚度和位移都受底部留白限制，不改变按键布局、文字字号或点击范围。文字、图标与提示跟随键帽移动，位移使用 `dp`，不随系统字号放大。

样式只应用于好好主题，其他 Trime 主题沿用各自配置。立体模式复用原生 Canvas 与主题颜色，不引入持续动画或 3D 引擎；几何缓存随布局失效，配色随现有主题更新机制刷新。

词本头部和复习按钮显式关闭静态阴影，通过背景色、细分隔线与按压波纹表达层次和交互。原生对话框保留平台交互，不把网页手机示意外框的阴影带入应用内容。

## Shapes

筛选按钮使用柔和圆角（10dp），普通操作按钮使用 12dp 圆角，复习评分与揭晓按钮使用 16dp 圆角，搜索容器使用 14dp 圆角。单词列表保持平面行结构，避免给每一条义项再加嵌套卡片。

词本管理按钮复用 10dp 圆角分段背景：次级操作保留细边框，主要操作使用蜂蜜色与对应前景色；显式关闭静态阴影。学习首页的薄荷摘要使用 20dp 圆角与 20dp 内边距，内部操作仍按内容增高，不将摘要容器复制成逐词卡片。

## Components

- **单词行**：英文在前，中文、可选 IPA 和状态在后；发音与收藏是独立图标按钮。点击行打开义项详情，图标须有操作说明。
- **筛选与搜索**：「最近遇见」「我的收藏」「正在学习」用选中态区分；搜索支持中文和英文，空结果与尚无数据有各自说明。
- **义项详情**：呈现来源和保存提示；收藏与加入学习为独立操作，首次保存需确认当前义项。
- **复习**：支持英译中、中译英、拼写练习和混合回忆；先呈现问题，查看答案或核对拼写后才显示三个反馈。拼错或直接查看答案时禁用「记住了」，同时说明可选反馈。退出与系统返回可用，已完成反馈持久保存。
- **计划**：开启后首先显示今日新词、到期词和剩余数量；设置独立进入，保存不自动开始复习。数字输入框有标签和范围错误提示，开关配色复用好好资源。
- **学习首页主操作**：已有未完成会话优先显示继续；未保存单词时提供蜂蜜色「选择内置词书」和次级「导入我的词」；已有词但未开启计划时显示「设置每日数量」，有待办时显示开始，完成时说明休息。已保存但未加入学习时，明确引导进入词本主动加入；当前会话模式与下次生效的模式分别说明。
- **内置词书与个人词本**：目录按书名、真实词数、资料版本及添加按钮纵向排列，已添加项改为打开词本；页面说明共享进度与资料范围。个人词本展示共计、在学、未学和到期数量；搜索、词条选择、详情与批量操作各有明确入口。列表按 50 项分页，翻页清空所选项并显示当前页与总项数；删除词本前说明只删除分组，保留单词和学习进度。
- **导入预览**：先确定目标词本，再选 CSV 或粘贴中英两列；列顺序可切换。确认前显示新增、已存在、文件内重复和无效数量，预览与错误清单分别分页，错误带行号和原因。已有项明确说明保留本机内容与进度；有效项才提供蜂蜜色确认入口，并再次说明只保存、不自动加入学习。处理中以文字说明且阻止重复操作，完成和失败结果使用文字及无障碍实时区域反馈。
- **任务与恢复**：首屏显示实际任务数量或下次时间；共享会话标注模式和方向。复习与完成页提供单步撤销，发音异常提供重试和系统语音设置入口。
- **设置与工具箱**：常用入口使用用户能理解的名称，底层配置置于高级设置。工具栏编辑在一个可滚动面板内完成勾选、取消和排序；单手模式直接选择，避免再打开一层弹窗。
- **空态与完成**：金毛图像为装饰，不重复进入无障碍阅读顺序；主文案说明当前状态，辅助文案给出下一步。
- **学习统计与我的学习**：学习足迹和复习安排使用真实数量柱状图；跨日回忆自评使用独立数据点，缺样本区间留空，不插值连接或绘制遗忘预测。无跨日样本时直接说明，图表同时提供无障碍描述和可展开的逐项数据；打卡、反馈和积累只展示本机真实记录，长间隔复习不表述为永久掌握。
- **薄弱词**：从学习首页进入，先解释独立加练与记录口径；「最近总忘」「经常拼错」分别显示真实词数、义项和失败次数，各按 50 词分页，重建保留各自页码。已有会话提供「继续上次加练」，空列表说明暂无需专项练习的词，历史未知拼写结果明确说明不作猜测。
- **专项加练**：沿用复习页的题面、反馈与撤销，首屏显示「专项加练」、真实进度、模式和独立说明；每轮最多十词，最近总忘采用混合回忆，经常拼错使用拼写。反向题在揭晓前隐藏英文答案；拼写差异在核对后解释。完成页呈现值得再练的词并提供单步撤销，不自动开始下一轮；文案明确不占每日额度、不改变正式复习日期。
- **备份与恢复**：从「我的学习」进入，先说明文件内容、保存责任和限制，使用系统文档选择器导出或读取。校验成功后显示备份时间及词、句、正式记录、加练记录的真实数量；「完整恢复」与「仅合并单词和句子」分别列为操作，并用原生确认框说明各自影响。已有副本时提供「撤回上次完整恢复」，执行前说明会替换恢复后新增内容且副本仅可用一次；处理中显示文字状态并防止重复操作，结果通过文字和无障碍实时区域告知。

实现入口：[词本布局](app/src/main/res/layout/fragment_input_footprints.xml)、[单词行](app/src/main/res/layout/item_input_footprint.xml)、[详情与复习](app/src/main/java/com/osfans/trime/ui/main/footprints/WordLearningActivity.kt)、[学习图表](app/src/main/java/com/osfans/trime/ui/main/footprints/LearningChartView.kt)、[学习状态文案](app/src/main/res/values/learning_progress_strings.xml)。

本轮补充依据：[备份与恢复](app/src/main/java/com/osfans/trime/ui/main/footprints/LearningBackupActivity.kt)、[键帽绘制](app/src/main/java/com/osfans/trime/ime/keyboard/KeyView.kt)、[备份与专项文案](app/src/main/res/values-zh-rCN/learning_next_strings.xml)。分页、抽题和恢复数据语义以[个人词本业务规则](docs/business/personal-words.md)为准，不提升为其他界面的统一策略。

词本扩展依据：[词本管理与导入](app/src/main/java/com/osfans/trime/ui/main/footprints/WordbookActivity.kt)、[词本文案](app/src/main/res/values/wordbook_strings.xml)及[设备截图](work/wordbooks-validation/review/)。保存、分组、收藏、加入学习在界面上使用独立动作和影响说明，不能用「已保存」暗示已排入学习；三本资料的词数和版本属于内容事实，不作为设计令牌。

## Do's and Don'ts

- **Do** 保留 Android 原生返回、触控反馈、系统字号与明暗主题适配。
- **Do** 用义项内容和用户主动回答解释状态；学习文案保持克制。
- **Don't** 把遇见次数写成记忆程度、把完成一轮写成已经掌握。
- **Don't** 在正常键盘、单词列表或答题内容中反复放置金毛装饰。
- **Don't** 把 HTML 示例、临时失败截图或未验证的显示效果固化为原生设计规则。
